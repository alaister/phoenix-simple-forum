<%= form_for @changeset, @action, fn f -> %>
  <%= label f, :title, "Title:" %>
  <%= text_input f, :title %>
  <%= error_tag f, :title %>

  <%= label f, :content, "Content:" %>
  <%= textarea f, :content %>
  <%= error_tag f, :content %>

  <%= submit "Submit" %>
<% end %>